version: '3'
services:
  web:
    image: playground/buildkite
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    volumes:
      - ./public:/app/public
      - ./src:/app/src
      - ./build:/app/build
    command: npm run build
